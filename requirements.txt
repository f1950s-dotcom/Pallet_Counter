# Required at runtime (core pipeline, sample video, dashboard)
numpy>=1.24
opencv-python-headless>=4.8

# Optional: only if you run detection with a trained YOLO model
#   pip install ultralytics
# Dev: run tests
#   pip install pytest
